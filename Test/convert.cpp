#include <bits/stdc++.h>
#include <string>
using namespace std;

string convert(string s) {
    // vector<int> v;
    // int len = s.length();
    // cout << len << endl;
    // for (int i = 0; i < len; i++) {
    //     cout << s[i];
    // }

    // // convert string to integer vector using transform
    // transform(s.begin(), s.end(), back_inserter(v), [](char c) { return c - '0'; });
    // for (int i = 0; i < len; i++) {
    //     cout << v[i];
    // }
    return s;
}

int main() {
    cout << convert("123456789");   // use this to print string to the terminal
}